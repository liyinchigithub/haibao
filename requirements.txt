# 核心依赖
Flask==2.2.3         # Web框架 (兼容Python 3.8)
Pillow==9.5.0        # 图片处理库 (支持中文渲染)
Werkzeug==2.2.3      # Flask依赖项 (处理文件上传)

# 可选增强库（按需启用）
python-dotenv==1.0.0 # 环境变量管理
MarkupSafe==2.1.3    # 模板安全转义
Jinja2==3.1.2        # 模板引擎
flask-cors==5.0.0